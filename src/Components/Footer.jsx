import React from 'react';

const Footer = () => {
  return (
    <footer style={styles.footer}>
      <p></p>
    </footer>
  );
};

const styles = {
  footer: { textAlign: 'center', padding: '20px', backgroundColor: '#343a40', color: '#fff', marginTop: '40px' }
};

export default Footer;